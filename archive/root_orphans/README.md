# Archived root modules

Moved out of repo root during maintainability cleanup (2026-08).

- `hermes_agent.py` — zero importers
- `x_account_analyzer.py` — zero importers

Do not re-add to root; wire via `hermes/` or delete after one release cycle.

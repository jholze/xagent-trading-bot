## Sidebar (required)

- [ ] Labels: exactly one type (`bug` / `enhancement` / `type:ops` / `documentation`) + one allowlisted `area:*` + one `priority:p0|p1|p2` (copied from the issue)
- [ ] Assignee: `jholze`
- [ ] Project: Trading Bot Kanban
- [ ] Body contains `Fixes #<n>` for every ticket this PR ships

Issue remains the Kanban card. Do not treat this PR as a board work item.

## Summary

Fixes #

## Test plan

- [ ]

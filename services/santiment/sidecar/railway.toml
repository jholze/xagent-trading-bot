# Deploy with Railway root = repository root and Dockerfile path:
#   services/santiment_sidecar/Dockerfile
[build]
builder = "DOCKERFILE"
dockerfilePath = "services/santiment_sidecar/Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

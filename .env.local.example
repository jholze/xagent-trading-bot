# Local Mac development — copy relevant lines to your shell profile or run:
#   source scripts/dev_local_mongo.sh
#
# NEVER export Railway MONGO_URL in your local shell — pytest would wipe Railway data.

# unset MONGO_URL
# export MONGODB_URI=mongodb://127.0.0.1:27017
# export MONGODB_DB=xagent_test
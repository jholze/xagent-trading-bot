[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: slow end-to-end stress tests (network, optional)
    telegram_e2e: optional Telethon live E2E (requires TELEGRAM_E2E=1 + secrets)
addopts = -ra
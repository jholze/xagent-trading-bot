# Core dependencies for the X-Agent Trading Bot
python-dotenv>=1.0.0
Flask>=2.3.0
# MCP sidecar FastMCP (streamable HTTP). Pin <2: v2 renamed FastMCP → MCPServer.
mcp>=1.21.0,<2
requests>=2.28.0
ccxt>=4.0.0
pandas>=2.0.0
rich>=13.0.0
openai>=1.0.0
pytest>=8.0.0
matplotlib>=3.8.0
pymongo>=4.6.0
redis>=5.0.0
cryptography>=42.0.0

# Technical analysis — 0.6+ ships manylinux wheels (no system C compile in Docker).
# Local macOS still: brew install ta-lib if building from source.
TA-Lib>=0.6.0
websocket-client>=1.6.0

# Core dependencies for the X-Agent Trading Bot
python-dotenv>=1.0.0
Flask>=2.3.0
requests>=2.28.0
ccxt>=4.0.0
pandas>=2.0.0
rich>=13.0.0
openai>=1.0.0

# Technical analysis - requires TA-Lib C library to be installed on the system first
# On macOS: brew install ta-lib
# On Ubuntu: apt-get install -y libta-lib-dev
TA-Lib>=0.4.28
